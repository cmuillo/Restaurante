# Variables de entorno para el frontend
# Copiar a .env.local

# URL del API (en producción apunta al backend)
VITE_API_URL=http://localhost:3000

# branchId fijo para el kiosco (UUID de la sucursal)
VITE_KIOSK_BRANCH_ID=
